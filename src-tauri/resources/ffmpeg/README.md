# FFmpeg Resource

This directory is a local development fallback for the custom screen recorder.

Run:

```bash
pnpm ffmpeg:install
```

The downloaded executable is intentionally ignored by Git. To generate an
installable resource plugin package, run:

```bash
pnpm ffmpeg:package
```
