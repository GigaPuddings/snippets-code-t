import { i as ft, a as $e, b as Ue, U as He, u as Tt, m as St, d as Ce, c as nt, e as It, f as ze, g as Et, _ as Ze, h as Te, j as Pt, k as Vt, l as Nt, n as Mt, o as Wt, V as Dt, v as Ot, p as Bt, q as jt, r as At, s as Rt, E as fe, t as $t, I as at, C as lt, w as pt, x as Ht, y as Ft, z as Ut, S as B, R as Zt, A as s, B as ie, D as Gt, F as Kt, G as qt, T as R, H as Jt, J as pe, K as Qt, L as Yt, M as Xt, N as en, O as tn, P as nn } from "./_plugin-vue_export-helper-C3e6AB_k.js";
import { I as te, c as d, i as De, a as z, g as an, d as oe, u as ln, b as sn, s as Oe, r as N, w as Le, n as se, o as Ge, t as gt, e as m, f as h, h as k, F as Z, j as b, k as L, l, m as de, p as I, q as O, v as ge, x as Se, y as on, N as rn, z as ee, A as it, B as Ie, C as ve, D as un, E as cn, G as Be } from "./runtime-entry-BYS4P8M5.js";
const dn = te("close-small", !1, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M14 14L34 34",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M14 34L34 14",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), hn = te("copy", !0, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M13 12.4316V7.8125C13 6.2592 14.2592 5 15.8125 5H40.1875C41.7408 5 43 6.2592 43 7.8125V32.1875C43 33.7408 41.7408 35 40.1875 35H35.5163",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M32.1875 13H7.8125C6.2592 13 5 14.2592 5 15.8125V40.1875C5 41.7408 6.2592 43 7.8125 43H32.1875C33.7408 43 35 41.7408 35 40.1875V15.8125C35 14.2592 33.7408 13 32.1875 13Z",
    fill: t.colors[1],
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), fn = te("delete", !1, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M9 10V44H39V10H9Z",
    fill: t.colors[1],
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M20 20V33",
    stroke: t.colors[2],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M28 20V33",
    stroke: t.colors[2],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M4 10H44",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M16 10L19.289 4H28.7771L32 10H16Z",
    fill: t.colors[1],
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), pn = te("down", !1, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M36 18L24 30L12 18",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), gn = te("pushpin", !1, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M32 4H16L20 7L16 20C16 20 10 24 10 28H20L24 44L28 28H38C38 24 34 21.1667 32 20L28 7L32 4Z",
    fill: t.colors[1],
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), st = te("switch", !0, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M42 19H5.99998",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M30 7L42 19",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M6.79897 29H42.799",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M6.79895 29L18.799 41",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), vn = te("up", !1, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M13 30L25 18L37 30",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), ot = te("volume-up", !0, function(t) {
  return d("svg", {
    width: t.size,
    height: t.size,
    viewBox: "0 0 48 48",
    fill: "none"
  }, [d("path", {
    d: "M24 6V42C17 42 11.7985 32.8391 11.7985 32.8391H6C4.89543 32.8391 4 31.9437 4 30.8391V17.0108C4 15.9062 4.89543 15.0108 6 15.0108H11.7985C11.7985 15.0108 17 6 24 6Z",
    fill: t.colors[1],
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M32 24H44",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null), d("path", {
    d: "M38 18V30",
    stroke: t.colors[0],
    "stroke-width": t.strokeWidth,
    "stroke-linecap": t.strokeLinecap,
    "stroke-linejoin": t.strokeLinejoin
  }, null)]);
}), wn = () => ft && /firefox/i.test(window.navigator.userAgent);
let V;
const bn = {
  height: "0",
  visibility: "hidden",
  overflow: wn() ? "" : "hidden",
  position: "absolute",
  "z-index": "-1000",
  top: "0",
  right: "0"
}, yn = [
  "letter-spacing",
  "line-height",
  "padding-top",
  "padding-bottom",
  "font-family",
  "font-weight",
  "font-size",
  "text-rendering",
  "text-transform",
  "width",
  "text-indent",
  "padding-left",
  "padding-right",
  "border-width",
  "box-sizing"
];
function mn(t) {
  const e = window.getComputedStyle(t), n = e.getPropertyValue("box-sizing"), a = Number.parseFloat(e.getPropertyValue("padding-bottom")) + Number.parseFloat(e.getPropertyValue("padding-top")), r = Number.parseFloat(e.getPropertyValue("border-bottom-width")) + Number.parseFloat(e.getPropertyValue("border-top-width"));
  return { contextStyle: yn.map((u) => [
    u,
    e.getPropertyValue(u)
  ]), paddingSize: a, borderSize: r, boxSizing: n };
}
function rt(t, e = 1, n) {
  var a;
  V || (V = document.createElement("textarea"), document.body.appendChild(V));
  const { paddingSize: r, borderSize: c, boxSizing: u, contextStyle: w } = mn(t);
  w.forEach(([x, G]) => V?.style.setProperty(x, G)), Object.entries(bn).forEach(([x, G]) => V?.style.setProperty(x, G, "important")), V.value = t.value || t.placeholder || "";
  let v = V.scrollHeight;
  const E = {};
  u === "border-box" ? v = v + c : u === "content-box" && (v = v - r), V.value = "";
  const _ = V.scrollHeight - r;
  if ($e(e)) {
    let x = _ * e;
    u === "border-box" && (x = x + r + c), v = Math.max(x, v), E.minHeight = `${x}px`;
  }
  if ($e(n)) {
    let x = _ * n;
    u === "border-box" && (x = x + r + c), v = Math.min(x, v);
  }
  return E.height = `${v}px`, (a = V.parentNode) == null || a.removeChild(V), V = void 0, E;
}
const kn = Ue({
  id: {
    type: String,
    default: void 0
  },
  size: It,
  disabled: Boolean,
  modelValue: {
    type: Ce([
      String,
      Number,
      Object
    ]),
    default: ""
  },
  maxlength: {
    type: [String, Number]
  },
  minlength: {
    type: [String, Number]
  },
  type: {
    type: String,
    default: "text"
  },
  resize: {
    type: String,
    values: ["none", "both", "horizontal", "vertical"]
  },
  autosize: {
    type: Ce([Boolean, Object]),
    default: !1
  },
  autocomplete: {
    type: String,
    default: "off"
  },
  formatter: {
    type: Function
  },
  parser: {
    type: Function
  },
  placeholder: {
    type: String
  },
  form: {
    type: String
  },
  readonly: Boolean,
  clearable: Boolean,
  showPassword: Boolean,
  showWordLimit: Boolean,
  suffixIcon: {
    type: nt
  },
  prefixIcon: {
    type: nt
  },
  containerRole: {
    type: String,
    default: void 0
  },
  tabindex: {
    type: [String, Number],
    default: 0
  },
  validateEvent: {
    type: Boolean,
    default: !0
  },
  inputStyle: {
    type: Ce([Object, Array, String]),
    default: () => St({})
  },
  autofocus: Boolean,
  rows: {
    type: Number,
    default: 2
  },
  ...Tt(["ariaLabel"])
}), _n = {
  [He]: (t) => De(t),
  input: (t) => De(t),
  change: (t) => De(t),
  focus: (t) => t instanceof FocusEvent,
  blur: (t) => t instanceof FocusEvent,
  clear: () => !0,
  mouseleave: (t) => t instanceof MouseEvent,
  mouseenter: (t) => t instanceof MouseEvent,
  keydown: (t) => t instanceof Event,
  compositionstart: (t) => t instanceof CompositionEvent,
  compositionupdate: (t) => t instanceof CompositionEvent,
  compositionend: (t) => t instanceof CompositionEvent
}, xn = ["class", "style"], Cn = /^on[A-Z]/, zn = (t = {}) => {
  const { excludeListeners: e = !1, excludeKeys: n } = t, a = z(() => (n?.value || []).concat(xn)), r = an();
  return r ? z(() => {
    var c;
    return Et(Object.entries((c = r.proxy) == null ? void 0 : c.$attrs).filter(([u]) => !a.value.includes(u) && !(e && Cn.test(u))));
  }) : (ze("use-attrs", "getCurrentInstance() returned null. useAttrs() must be called at the top of a setup function"), z(() => ({})));
};
function Ln(t) {
  let e;
  function n() {
    if (t.value == null)
      return;
    const { selectionStart: r, selectionEnd: c, value: u } = t.value;
    if (r == null || c == null)
      return;
    const w = u.slice(0, Math.max(0, r)), v = u.slice(Math.max(0, c));
    e = {
      selectionStart: r,
      selectionEnd: c,
      value: u,
      beforeTxt: w,
      afterTxt: v
    };
  }
  function a() {
    if (t.value == null || e == null)
      return;
    const { value: r } = t.value, { beforeTxt: c, afterTxt: u, selectionStart: w } = e;
    if (c == null || u == null || w == null)
      return;
    let v = r.length;
    if (r.endsWith(u))
      v = r.length - u.length;
    else if (r.startsWith(c))
      v = c.length;
    else {
      const E = c[w - 1], _ = r.indexOf(E, w - 1);
      _ !== -1 && (v = _ + 1);
    }
    t.value.setSelectionRange(v, v);
  }
  return [n, a];
}
const vt = "ElInput", Tn = oe({
  name: vt,
  inheritAttrs: !1
}), Sn = /* @__PURE__ */ oe({
  ...Tn,
  props: kn,
  emits: _n,
  setup(t, { expose: e, emit: n }) {
    const a = t, r = ln(), c = zn(), u = sn(), w = z(() => [
      a.type === "textarea" ? D.b() : p.b(),
      p.m(G.value),
      p.is("disabled", H.value),
      p.is("exceed", We.value),
      {
        [p.b("group")]: u.prepend || u.append,
        [p.m("prefix")]: u.prefix || a.prefixIcon,
        [p.m("suffix")]: u.suffix || a.suffixIcon || a.clearable || a.showPassword,
        [p.bm("suffix", "password-clear")]: J.value && he.value,
        [p.b("hidden")]: a.type === "hidden"
      },
      r.class
    ]), v = z(() => [
      p.e("wrapper"),
      p.is("focus", re.value)
    ]), { form: E, formItem: _ } = Mt(), { inputId: x } = Wt(a, {
      formItemContext: _
    }), G = Pt(), H = Vt(), p = Te("input"), D = Te("textarea"), j = Oe(), P = Oe(), $ = N(!1), ne = N(!1), ye = N(), F = Oe(a.inputStyle), K = z(() => j.value || P.value), { wrapperRef: Ve, isFocused: re, handleFocus: Ne, handleBlur: me } = Nt(K, {
      beforeFocus() {
        return H.value;
      },
      afterBlur() {
        var i;
        a.validateEvent && ((i = _?.validate) == null || i.call(_, "blur").catch((y) => ze(y)));
      }
    }), ae = z(() => {
      var i;
      return (i = E?.statusIcon) != null ? i : !1;
    }), le = z(() => _?.validateState || ""), ke = z(() => le.value && Dt[le.value]), q = z(() => ne.value ? Ot : Bt), Me = z(() => [
      r.style
    ]), _e = z(() => [
      a.inputStyle,
      F.value,
      { resize: a.resize }
    ]), A = z(() => jt(a.modelValue) ? "" : String(a.modelValue)), J = z(() => a.clearable && !H.value && !a.readonly && !!A.value && (re.value || $.value)), he = z(() => a.showPassword && !H.value && !!A.value && (!!A.value || re.value)), Q = z(() => a.showWordLimit && !!a.maxlength && (a.type === "text" || a.type === "textarea") && !H.value && !a.readonly && !a.showPassword), Y = z(() => A.value.length), We = z(() => !!Q.value && Y.value > Number(a.maxlength)), o = z(() => !!u.suffix || !!a.suffixIcon || J.value || a.showPassword || Q.value || !!le.value && ae.value), [f, T] = Ln(j);
    At(P, (i) => {
      if (C(), !Q.value || a.resize !== "both")
        return;
      const y = i[0], { width: ue } = y.contentRect;
      ye.value = {
        right: `calc(100% - ${ue + 15 + 6}px)`
      };
    });
    const S = () => {
      const { type: i, autosize: y } = a;
      if (!(!ft || i !== "textarea" || !P.value))
        if (y) {
          const ue = Ie(y) ? y.minRows : void 0, et = Ie(y) ? y.maxRows : void 0, tt = rt(P.value, ue, et);
          F.value = {
            overflowY: "hidden",
            ...tt
          }, se(() => {
            P.value.offsetHeight, F.value = tt;
          });
        } else
          F.value = {
            minHeight: rt(P.value).minHeight
          };
    }, C = ((i) => {
      let y = !1;
      return () => {
        var ue;
        if (y || !a.autosize)
          return;
        ((ue = P.value) == null ? void 0 : ue.offsetParent) === null || (i(), y = !0);
      };
    })(S), W = () => {
      const i = K.value, y = a.formatter ? a.formatter(A.value) : A.value;
      !i || i.value === y || (i.value = y);
    }, g = async (i) => {
      f();
      let { value: y } = i.target;
      if (a.formatter && a.parser && (y = a.parser(y)), !Ke.value) {
        if (y === A.value) {
          W();
          return;
        }
        n(He, y), n(at, y), await se(), W(), T();
      }
    }, U = (i) => {
      let { value: y } = i.target;
      a.formatter && a.parser && (y = a.parser(y)), n(lt, y);
    }, {
      isComposing: Ke,
      handleCompositionStart: qe,
      handleCompositionUpdate: Je,
      handleCompositionEnd: Qe
    } = Rt({ emit: n, afterComposition: g }), kt = () => {
      f(), ne.value = !ne.value, setTimeout(T);
    }, _t = () => {
      var i;
      return (i = K.value) == null ? void 0 : i.focus();
    }, xt = () => {
      var i;
      return (i = K.value) == null ? void 0 : i.blur();
    }, Ct = (i) => {
      $.value = !1, n("mouseleave", i);
    }, zt = (i) => {
      $.value = !0, n("mouseenter", i);
    }, Ye = (i) => {
      n("keydown", i);
    }, Lt = () => {
      var i;
      (i = K.value) == null || i.select();
    }, Xe = () => {
      n(He, ""), n(lt, ""), n("clear"), n(at, "");
    };
    return Le(() => a.modelValue, () => {
      var i;
      se(() => S()), a.validateEvent && ((i = _?.validate) == null || i.call(_, "change").catch((y) => ze(y)));
    }), Le(A, () => W()), Le(() => a.type, async () => {
      await se(), W(), S();
    }), Ge(() => {
      !a.formatter && a.parser && ze(vt, "If you set the parser, you also need to set the formatter."), W(), se(S);
    }), e({
      input: j,
      textarea: P,
      ref: K,
      textareaStyle: _e,
      autosize: gt(a, "autosize"),
      isComposing: Ke,
      focus: _t,
      blur: xt,
      select: Lt,
      clear: Xe,
      resizeTextarea: S
    }), (i, y) => (h(), m("div", {
      class: L([
        l(w),
        {
          [l(p).bm("group", "append")]: i.$slots.append,
          [l(p).bm("group", "prepend")]: i.$slots.prepend
        }
      ]),
      style: it(l(Me)),
      onMouseenter: zt,
      onMouseleave: Ct
    }, [
      k(" input "),
      i.type !== "textarea" ? (h(), m(Z, { key: 0 }, [
        k(" prepend slot "),
        i.$slots.prepend ? (h(), m("div", {
          key: 0,
          class: L(l(p).be("group", "prepend"))
        }, [
          de(i.$slots, "prepend")
        ], 2)) : k("v-if", !0),
        b("div", {
          ref_key: "wrapperRef",
          ref: Ve,
          class: L(l(v))
        }, [
          k(" prefix slot "),
          i.$slots.prefix || i.prefixIcon ? (h(), m("span", {
            key: 0,
            class: L(l(p).e("prefix"))
          }, [
            b("span", {
              class: L(l(p).e("prefix-inner"))
            }, [
              de(i.$slots, "prefix"),
              i.prefixIcon ? (h(), I(l(fe), {
                key: 0,
                class: L(l(p).e("icon"))
              }, {
                default: O(() => [
                  (h(), I(ge(i.prefixIcon)))
                ]),
                _: 1
              }, 8, ["class"])) : k("v-if", !0)
            ], 2)
          ], 2)) : k("v-if", !0),
          b("input", Se({
            id: l(x),
            ref_key: "input",
            ref: j,
            class: l(p).e("inner")
          }, l(c), {
            minlength: i.minlength,
            maxlength: i.maxlength,
            type: i.showPassword ? ne.value ? "text" : "password" : i.type,
            disabled: l(H),
            readonly: i.readonly,
            autocomplete: i.autocomplete,
            tabindex: i.tabindex,
            "aria-label": i.ariaLabel,
            placeholder: i.placeholder,
            style: i.inputStyle,
            form: i.form,
            autofocus: i.autofocus,
            role: i.containerRole,
            onCompositionstart: l(qe),
            onCompositionupdate: l(Je),
            onCompositionend: l(Qe),
            onInput: g,
            onChange: U,
            onKeydown: Ye
          }), null, 16, ["id", "minlength", "maxlength", "type", "disabled", "readonly", "autocomplete", "tabindex", "aria-label", "placeholder", "form", "autofocus", "role", "onCompositionstart", "onCompositionupdate", "onCompositionend"]),
          k(" suffix slot "),
          l(o) ? (h(), m("span", {
            key: 1,
            class: L(l(p).e("suffix"))
          }, [
            b("span", {
              class: L(l(p).e("suffix-inner"))
            }, [
              !l(J) || !l(he) || !l(Q) ? (h(), m(Z, { key: 0 }, [
                de(i.$slots, "suffix"),
                i.suffixIcon ? (h(), I(l(fe), {
                  key: 0,
                  class: L(l(p).e("icon"))
                }, {
                  default: O(() => [
                    (h(), I(ge(i.suffixIcon)))
                  ]),
                  _: 1
                }, 8, ["class"])) : k("v-if", !0)
              ], 64)) : k("v-if", !0),
              l(J) ? (h(), I(l(fe), {
                key: 1,
                class: L([l(p).e("icon"), l(p).e("clear")]),
                onMousedown: on(l(rn), ["prevent"]),
                onClick: Xe
              }, {
                default: O(() => [
                  d(l($t))
                ]),
                _: 1
              }, 8, ["class", "onMousedown"])) : k("v-if", !0),
              l(he) ? (h(), I(l(fe), {
                key: 2,
                class: L([l(p).e("icon"), l(p).e("password")]),
                onClick: kt
              }, {
                default: O(() => [
                  (h(), I(ge(l(q))))
                ]),
                _: 1
              }, 8, ["class"])) : k("v-if", !0),
              l(Q) ? (h(), m("span", {
                key: 3,
                class: L(l(p).e("count"))
              }, [
                b("span", {
                  class: L(l(p).e("count-inner"))
                }, ee(l(Y)) + " / " + ee(i.maxlength), 3)
              ], 2)) : k("v-if", !0),
              l(le) && l(ke) && l(ae) ? (h(), I(l(fe), {
                key: 4,
                class: L([
                  l(p).e("icon"),
                  l(p).e("validateIcon"),
                  l(p).is("loading", l(le) === "validating")
                ])
              }, {
                default: O(() => [
                  (h(), I(ge(l(ke))))
                ]),
                _: 1
              }, 8, ["class"])) : k("v-if", !0)
            ], 2)
          ], 2)) : k("v-if", !0)
        ], 2),
        k(" append slot "),
        i.$slots.append ? (h(), m("div", {
          key: 1,
          class: L(l(p).be("group", "append"))
        }, [
          de(i.$slots, "append")
        ], 2)) : k("v-if", !0)
      ], 64)) : (h(), m(Z, { key: 1 }, [
        k(" textarea "),
        b("textarea", Se({
          id: l(x),
          ref_key: "textarea",
          ref: P,
          class: [l(D).e("inner"), l(p).is("focus", l(re))]
        }, l(c), {
          minlength: i.minlength,
          maxlength: i.maxlength,
          tabindex: i.tabindex,
          disabled: l(H),
          readonly: i.readonly,
          autocomplete: i.autocomplete,
          style: l(_e),
          "aria-label": i.ariaLabel,
          placeholder: i.placeholder,
          form: i.form,
          autofocus: i.autofocus,
          rows: i.rows,
          role: i.containerRole,
          onCompositionstart: l(qe),
          onCompositionupdate: l(Je),
          onCompositionend: l(Qe),
          onInput: g,
          onFocus: l(Ne),
          onBlur: l(me),
          onChange: U,
          onKeydown: Ye
        }), null, 16, ["id", "minlength", "maxlength", "tabindex", "disabled", "readonly", "autocomplete", "aria-label", "placeholder", "form", "autofocus", "rows", "role", "onCompositionstart", "onCompositionupdate", "onCompositionend", "onFocus", "onBlur"]),
        l(Q) ? (h(), m("span", {
          key: 0,
          style: it(ye.value),
          class: L(l(p).e("count"))
        }, ee(l(Y)) + " / " + ee(i.maxlength), 7)) : k("v-if", !0)
      ], 64))
    ], 38));
  }
});
var In = /* @__PURE__ */ Ze(Sn, [["__file", "input.vue"]]);
const En = pt(In), Pn = Ue({
  animated: {
    type: Boolean,
    default: !1
  },
  count: {
    type: Number,
    default: 1
  },
  rows: {
    type: Number,
    default: 3
  },
  loading: {
    type: Boolean,
    default: !0
  },
  throttle: {
    type: Ce([Number, Object])
  }
}), Vn = Ue({
  variant: {
    type: String,
    values: [
      "circle",
      "rect",
      "h1",
      "h3",
      "text",
      "caption",
      "p",
      "image",
      "button"
    ],
    default: "text"
  }
}), Nn = oe({
  name: "ElSkeletonItem"
}), Mn = /* @__PURE__ */ oe({
  ...Nn,
  props: Vn,
  setup(t) {
    const e = Te("skeleton");
    return (n, a) => (h(), m("div", {
      class: L([l(e).e("item"), l(e).e(n.variant)])
    }, [
      n.variant === "image" ? (h(), I(l(Ht), { key: 0 })) : k("v-if", !0)
    ], 2));
  }
});
var Ee = /* @__PURE__ */ Ze(Mn, [["__file", "skeleton-item.vue"]]);
const Wn = (t, e = 0) => {
  if (e === 0)
    return t;
  const n = Ie(e) && !!e.initVal, a = N(n);
  let r = null;
  const c = (w) => {
    if (Ft(w)) {
      a.value = t.value;
      return;
    }
    r && clearTimeout(r), r = setTimeout(() => {
      a.value = t.value;
    }, w);
  }, u = (w) => {
    w === "leading" ? $e(e) ? c(e) : c(e.leading) : Ie(e) ? c(e.trailing) : a.value = !1;
  };
  return Ge(() => u("leading")), Le(() => t.value, (w) => {
    u(w ? "leading" : "trailing");
  }), a;
}, Dn = oe({
  name: "ElSkeleton"
}), On = /* @__PURE__ */ oe({
  ...Dn,
  props: Pn,
  setup(t, { expose: e }) {
    const n = t, a = Te("skeleton"), r = Wn(gt(n, "loading"), n.throttle);
    return e({
      uiLoading: r
    }), (c, u) => l(r) ? (h(), m("div", Se({
      key: 0,
      class: [l(a).b(), l(a).is("animated", c.animated)]
    }, c.$attrs), [
      (h(!0), m(Z, null, ve(c.count, (w) => (h(), m(Z, { key: w }, [
        l(r) ? de(c.$slots, "template", { key: w }, () => [
          d(Ee, {
            class: L(l(a).is("first")),
            variant: "p"
          }, null, 8, ["class"]),
          (h(!0), m(Z, null, ve(c.rows, (v) => (h(), I(Ee, {
            key: v,
            class: L([
              l(a).e("paragraph"),
              l(a).is("last", v === c.rows && c.rows > 1)
            ]),
            variant: "p"
          }, null, 8, ["class"]))), 128))
        ]) : k("v-if", !0)
      ], 64))), 128))
    ], 16)) : de(c.$slots, "default", un(Se({ key: 1 }, c.$attrs)));
  }
});
var Bn = /* @__PURE__ */ Ze(On, [["__file", "skeleton.vue"]]);
const jn = pt(Bn, {
  SkeletonItem: Ee
});
Ut(Ee);
class wt {
  constructor(...e) {
    this.type = "Logical", e.length === 1 ? "Logical" in e[0] ? (this.width = e[0].Logical.width, this.height = e[0].Logical.height) : (this.width = e[0].width, this.height = e[0].height) : (this.width = e[0], this.height = e[1]);
  }
  /**
   * Converts the logical size to a physical one.
   * @example
   * ```typescript
   * import { LogicalSize } from '@tauri-apps/api/dpi';
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   *
   * const appWindow = getCurrentWindow();
   * const factor = await appWindow.scaleFactor();
   * const size = new LogicalSize(400, 500);
   * const physical = size.toPhysical(factor);
   * ```
   *
   * @since 2.0.0
   */
  toPhysical(e) {
    return new we(this.width * e, this.height * e);
  }
  [B]() {
    return {
      width: this.width,
      height: this.height
    };
  }
  toJSON() {
    return this[B]();
  }
}
class we {
  constructor(...e) {
    this.type = "Physical", e.length === 1 ? "Physical" in e[0] ? (this.width = e[0].Physical.width, this.height = e[0].Physical.height) : (this.width = e[0].width, this.height = e[0].height) : (this.width = e[0], this.height = e[1]);
  }
  /**
   * Converts the physical size to a logical one.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const appWindow = getCurrentWindow();
   * const factor = await appWindow.scaleFactor();
   * const size = await appWindow.innerSize(); // PhysicalSize
   * const logical = size.toLogical(factor);
   * ```
   */
  toLogical(e) {
    return new wt(this.width / e, this.height / e);
  }
  [B]() {
    return {
      width: this.width,
      height: this.height
    };
  }
  toJSON() {
    return this[B]();
  }
}
class ce {
  constructor(e) {
    this.size = e;
  }
  toLogical(e) {
    return this.size instanceof wt ? this.size : this.size.toLogical(e);
  }
  toPhysical(e) {
    return this.size instanceof we ? this.size : this.size.toPhysical(e);
  }
  [B]() {
    return {
      [`${this.size.type}`]: {
        width: this.size.width,
        height: this.size.height
      }
    };
  }
  toJSON() {
    return this[B]();
  }
}
class bt {
  constructor(...e) {
    this.type = "Logical", e.length === 1 ? "Logical" in e[0] ? (this.x = e[0].Logical.x, this.y = e[0].Logical.y) : (this.x = e[0].x, this.y = e[0].y) : (this.x = e[0], this.y = e[1]);
  }
  /**
   * Converts the logical position to a physical one.
   * @example
   * ```typescript
   * import { LogicalPosition } from '@tauri-apps/api/dpi';
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   *
   * const appWindow = getCurrentWindow();
   * const factor = await appWindow.scaleFactor();
   * const position = new LogicalPosition(400, 500);
   * const physical = position.toPhysical(factor);
   * ```
   *
   * @since 2.0.0
   */
  toPhysical(e) {
    return new X(this.x * e, this.y * e);
  }
  [B]() {
    return {
      x: this.x,
      y: this.y
    };
  }
  toJSON() {
    return this[B]();
  }
}
class X {
  constructor(...e) {
    this.type = "Physical", e.length === 1 ? "Physical" in e[0] ? (this.x = e[0].Physical.x, this.y = e[0].Physical.y) : (this.x = e[0].x, this.y = e[0].y) : (this.x = e[0], this.y = e[1]);
  }
  /**
   * Converts the physical position to a logical one.
   * @example
   * ```typescript
   * import { PhysicalPosition } from '@tauri-apps/api/dpi';
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   *
   * const appWindow = getCurrentWindow();
   * const factor = await appWindow.scaleFactor();
   * const position = new PhysicalPosition(400, 500);
   * const physical = position.toLogical(factor);
   * ```
   *
   * @since 2.0.0
   */
  toLogical(e) {
    return new bt(this.x / e, this.y / e);
  }
  [B]() {
    return {
      x: this.x,
      y: this.y
    };
  }
  toJSON() {
    return this[B]();
  }
}
class xe {
  constructor(e) {
    this.position = e;
  }
  toLogical(e) {
    return this.position instanceof bt ? this.position : this.position.toLogical(e);
  }
  toPhysical(e) {
    return this.position instanceof X ? this.position : this.position.toPhysical(e);
  }
  [B]() {
    return {
      [`${this.position.type}`]: {
        x: this.position.x,
        y: this.position.y
      }
    };
  }
  toJSON() {
    return this[B]();
  }
}
class be extends Zt {
  /**
   * Creates an Image from a resource ID. For internal use only.
   *
   * @ignore
   */
  constructor(e) {
    super(e);
  }
  /** Creates a new Image using RGBA data, in row-major order from top to bottom, and with specified width and height. */
  static async new(e, n, a) {
    return s("plugin:image|new", {
      rgba: Pe(e),
      width: n,
      height: a
    }).then((r) => new be(r));
  }
  /**
   * Creates a new image using the provided bytes by inferring the file format.
   * If the format is known, prefer [@link Image.fromPngBytes] or [@link Image.fromIcoBytes].
   *
   * Only `ico` and `png` are supported (based on activated feature flag).
   *
   * Note that you need the `image-ico` or `image-png` Cargo features to use this API.
   * To enable it, change your Cargo.toml file:
   * ```toml
   * [dependencies]
   * tauri = { version = "...", features = ["...", "image-png"] }
   * ```
   */
  static async fromBytes(e) {
    return s("plugin:image|from_bytes", {
      bytes: Pe(e)
    }).then((n) => new be(n));
  }
  /**
   * Creates a new image using the provided path.
   *
   * Only `ico` and `png` are supported (based on activated feature flag).
   *
   * Note that you need the `image-ico` or `image-png` Cargo features to use this API.
   * To enable it, change your Cargo.toml file:
   * ```toml
   * [dependencies]
   * tauri = { version = "...", features = ["...", "image-png"] }
   * ```
   */
  static async fromPath(e) {
    return s("plugin:image|from_path", { path: e }).then((n) => new be(n));
  }
  /** Returns the RGBA data for this image, in row-major order from top to bottom.  */
  async rgba() {
    return s("plugin:image|rgba", {
      rid: this.rid
    }).then((e) => new Uint8Array(e));
  }
  /** Returns the size of this image.  */
  async size() {
    return s("plugin:image|size", { rid: this.rid });
  }
}
function Pe(t) {
  return t == null ? null : typeof t == "string" ? t : t instanceof be ? t.rid : t;
}
var Fe;
(function(t) {
  t[t.Critical = 1] = "Critical", t[t.Informational = 2] = "Informational";
})(Fe || (Fe = {}));
class An {
  constructor(e) {
    this._preventDefault = !1, this.event = e.event, this.id = e.id;
  }
  preventDefault() {
    this._preventDefault = !0;
  }
  isPreventDefault() {
    return this._preventDefault;
  }
}
var ut;
(function(t) {
  t.None = "none", t.Normal = "normal", t.Indeterminate = "indeterminate", t.Paused = "paused", t.Error = "error";
})(ut || (ut = {}));
function yt() {
  return new mt(window.__TAURI_INTERNALS__.metadata.currentWindow.label, {
    // @ts-expect-error `skip` is not defined in the public API but it is handled by the constructor
    skip: !0
  });
}
async function je() {
  return s("plugin:window|get_all_windows").then((t) => t.map((e) => new mt(e, {
    // @ts-expect-error `skip` is not defined in the public API but it is handled by the constructor
    skip: !0
  })));
}
const Ae = ["tauri://created", "tauri://error"];
class mt {
  /**
   * Creates a new Window.
   * @example
   * ```typescript
   * import { Window } from '@tauri-apps/api/window';
   * const appWindow = new Window('my-label');
   * appWindow.once('tauri://created', function () {
   *  // window successfully created
   * });
   * appWindow.once('tauri://error', function (e) {
   *  // an error happened creating the window
   * });
   * ```
   *
   * @param label The unique window label. Must be alphanumeric: `a-zA-Z-/:_`.
   * @returns The {@link Window} instance to communicate with the window.
   */
  constructor(e, n = {}) {
    var a;
    this.label = e, this.listeners = /* @__PURE__ */ Object.create(null), n?.skip || s("plugin:window|create", {
      options: {
        ...n,
        parent: typeof n.parent == "string" ? n.parent : (a = n.parent) === null || a === void 0 ? void 0 : a.label,
        label: e
      }
    }).then(async () => this.emit("tauri://created")).catch(async (r) => this.emit("tauri://error", r));
  }
  /**
   * Gets the Window associated with the given label.
   * @example
   * ```typescript
   * import { Window } from '@tauri-apps/api/window';
   * const mainWindow = Window.getByLabel('main');
   * ```
   *
   * @param label The window label.
   * @returns The Window instance to communicate with the window or null if the window doesn't exist.
   */
  static async getByLabel(e) {
    var n;
    return (n = (await je()).find((a) => a.label === e)) !== null && n !== void 0 ? n : null;
  }
  /**
   * Get an instance of `Window` for the current window.
   */
  static getCurrent() {
    return yt();
  }
  /**
   * Gets a list of instances of `Window` for all available windows.
   */
  static async getAll() {
    return je();
  }
  /**
   *  Gets the focused window.
   * @example
   * ```typescript
   * import { Window } from '@tauri-apps/api/window';
   * const focusedWindow = Window.getFocusedWindow();
   * ```
   *
   * @returns The Window instance or `undefined` if there is not any focused window.
   */
  static async getFocusedWindow() {
    for (const e of await je())
      if (await e.isFocused())
        return e;
    return null;
  }
  /**
   * Listen to an emitted event on this window.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const unlisten = await getCurrentWindow().listen<string>('state-changed', (event) => {
   *   console.log(`Got error: ${payload}`);
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @param event Event name. Must include only alphanumeric characters, `-`, `/`, `:` and `_`.
   * @param handler Event handler.
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async listen(e, n) {
    return this._handleTauriEvent(e, n) ? () => {
      const a = this.listeners[e];
      a.splice(a.indexOf(n), 1);
    } : ie(e, n, {
      target: { kind: "Window", label: this.label }
    });
  }
  /**
   * Listen to an emitted event on this window only once.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const unlisten = await getCurrentWindow().once<null>('initialized', (event) => {
   *   console.log(`Window initialized!`);
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @param event Event name. Must include only alphanumeric characters, `-`, `/`, `:` and `_`.
   * @param handler Event handler.
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async once(e, n) {
    return this._handleTauriEvent(e, n) ? () => {
      const a = this.listeners[e];
      a.splice(a.indexOf(n), 1);
    } : Gt(e, n, {
      target: { kind: "Window", label: this.label }
    });
  }
  /**
   * Emits an event to all {@link EventTarget|targets}.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().emit('window-loaded', { loggedIn: true, token: 'authToken' });
   * ```
   *
   * @param event Event name. Must include only alphanumeric characters, `-`, `/`, `:` and `_`.
   * @param payload Event payload.
   */
  async emit(e, n) {
    if (Ae.includes(e)) {
      for (const a of this.listeners[e] || [])
        a({
          event: e,
          id: -1,
          payload: n
        });
      return;
    }
    return Kt(e, n);
  }
  /**
   * Emits an event to all {@link EventTarget|targets} matching the given target.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().emit('main', 'window-loaded', { loggedIn: true, token: 'authToken' });
   * ```
   * @param target Label of the target Window/Webview/WebviewWindow or raw {@link EventTarget} object.
   * @param event Event name. Must include only alphanumeric characters, `-`, `/`, `:` and `_`.
   * @param payload Event payload.
   */
  async emitTo(e, n, a) {
    if (Ae.includes(n)) {
      for (const r of this.listeners[n] || [])
        r({
          event: n,
          id: -1,
          payload: a
        });
      return;
    }
    return qt(e, n, a);
  }
  /** @ignore */
  _handleTauriEvent(e, n) {
    return Ae.includes(e) ? (e in this.listeners ? this.listeners[e].push(n) : this.listeners[e] = [n], !0) : !1;
  }
  // Getters
  /**
   * The scale factor that can be used to map physical pixels to logical pixels.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const factor = await getCurrentWindow().scaleFactor();
   * ```
   *
   * @returns The window's monitor scale factor.
   */
  async scaleFactor() {
    return s("plugin:window|scale_factor", {
      label: this.label
    });
  }
  /**
   * The position of the top-left hand corner of the window's client area relative to the top-left hand corner of the desktop.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const position = await getCurrentWindow().innerPosition();
   * ```
   *
   * @returns The window's inner position.
   */
  async innerPosition() {
    return s("plugin:window|inner_position", {
      label: this.label
    }).then((e) => new X(e));
  }
  /**
   * The position of the top-left hand corner of the window relative to the top-left hand corner of the desktop.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const position = await getCurrentWindow().outerPosition();
   * ```
   *
   * @returns The window's outer position.
   */
  async outerPosition() {
    return s("plugin:window|outer_position", {
      label: this.label
    }).then((e) => new X(e));
  }
  /**
   * The physical size of the window's client area.
   * The client area is the content of the window, excluding the title bar and borders.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const size = await getCurrentWindow().innerSize();
   * ```
   *
   * @returns The window's inner size.
   */
  async innerSize() {
    return s("plugin:window|inner_size", {
      label: this.label
    }).then((e) => new we(e));
  }
  /**
   * The physical size of the entire window.
   * These dimensions include the title bar and borders. If you don't want that (and you usually don't), use inner_size instead.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const size = await getCurrentWindow().outerSize();
   * ```
   *
   * @returns The window's outer size.
   */
  async outerSize() {
    return s("plugin:window|outer_size", {
      label: this.label
    }).then((e) => new we(e));
  }
  /**
   * Gets the window's current fullscreen state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const fullscreen = await getCurrentWindow().isFullscreen();
   * ```
   *
   * @returns Whether the window is in fullscreen mode or not.
   */
  async isFullscreen() {
    return s("plugin:window|is_fullscreen", {
      label: this.label
    });
  }
  /**
   * Gets the window's current minimized state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const minimized = await getCurrentWindow().isMinimized();
   * ```
   */
  async isMinimized() {
    return s("plugin:window|is_minimized", {
      label: this.label
    });
  }
  /**
   * Gets the window's current maximized state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const maximized = await getCurrentWindow().isMaximized();
   * ```
   *
   * @returns Whether the window is maximized or not.
   */
  async isMaximized() {
    return s("plugin:window|is_maximized", {
      label: this.label
    });
  }
  /**
   * Gets the window's current focus state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const focused = await getCurrentWindow().isFocused();
   * ```
   *
   * @returns Whether the window is focused or not.
   */
  async isFocused() {
    return s("plugin:window|is_focused", {
      label: this.label
    });
  }
  /**
   * Gets the window's current decorated state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const decorated = await getCurrentWindow().isDecorated();
   * ```
   *
   * @returns Whether the window is decorated or not.
   */
  async isDecorated() {
    return s("plugin:window|is_decorated", {
      label: this.label
    });
  }
  /**
   * Gets the window's current resizable state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const resizable = await getCurrentWindow().isResizable();
   * ```
   *
   * @returns Whether the window is resizable or not.
   */
  async isResizable() {
    return s("plugin:window|is_resizable", {
      label: this.label
    });
  }
  /**
   * Gets the window's native maximize button state.
   *
   * #### Platform-specific
   *
   * - **Linux / iOS / Android:** Unsupported.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const maximizable = await getCurrentWindow().isMaximizable();
   * ```
   *
   * @returns Whether the window's native maximize button is enabled or not.
   */
  async isMaximizable() {
    return s("plugin:window|is_maximizable", {
      label: this.label
    });
  }
  /**
   * Gets the window's native minimize button state.
   *
   * #### Platform-specific
   *
   * - **Linux / iOS / Android:** Unsupported.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const minimizable = await getCurrentWindow().isMinimizable();
   * ```
   *
   * @returns Whether the window's native minimize button is enabled or not.
   */
  async isMinimizable() {
    return s("plugin:window|is_minimizable", {
      label: this.label
    });
  }
  /**
   * Gets the window's native close button state.
   *
   * #### Platform-specific
   *
   * - **iOS / Android:** Unsupported.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const closable = await getCurrentWindow().isClosable();
   * ```
   *
   * @returns Whether the window's native close button is enabled or not.
   */
  async isClosable() {
    return s("plugin:window|is_closable", {
      label: this.label
    });
  }
  /**
   * Gets the window's current visible state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const visible = await getCurrentWindow().isVisible();
   * ```
   *
   * @returns Whether the window is visible or not.
   */
  async isVisible() {
    return s("plugin:window|is_visible", {
      label: this.label
    });
  }
  /**
   * Gets the window's current title.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const title = await getCurrentWindow().title();
   * ```
   */
  async title() {
    return s("plugin:window|title", {
      label: this.label
    });
  }
  /**
   * Gets the window's current theme.
   *
   * #### Platform-specific
   *
   * - **macOS:** Theme was introduced on macOS 10.14. Returns `light` on macOS 10.13 and below.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const theme = await getCurrentWindow().theme();
   * ```
   *
   * @returns The window theme.
   */
  async theme() {
    return s("plugin:window|theme", {
      label: this.label
    });
  }
  /**
   * Whether the window is configured to be always on top of other windows or not.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * const alwaysOnTop = await getCurrentWindow().isAlwaysOnTop();
   * ```
   *
   * @returns Whether the window is visible or not.
   */
  async isAlwaysOnTop() {
    return s("plugin:window|is_always_on_top", {
      label: this.label
    });
  }
  // Setters
  /**
   * Centers the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().center();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async center() {
    return s("plugin:window|center", {
      label: this.label
    });
  }
  /**
   *  Requests user attention to the window, this has no effect if the application
   * is already focused. How requesting for user attention manifests is platform dependent,
   * see `UserAttentionType` for details.
   *
   * Providing `null` will unset the request for user attention. Unsetting the request for
   * user attention might not be done automatically by the WM when the window receives input.
   *
   * #### Platform-specific
   *
   * - **macOS:** `null` has no effect.
   * - **Linux:** Urgency levels have the same effect.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().requestUserAttention();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async requestUserAttention(e) {
    let n = null;
    return e && (e === Fe.Critical ? n = { type: "Critical" } : n = { type: "Informational" }), s("plugin:window|request_user_attention", {
      label: this.label,
      value: n
    });
  }
  /**
   * Updates the window resizable flag.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setResizable(false);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async setResizable(e) {
    return s("plugin:window|set_resizable", {
      label: this.label,
      value: e
    });
  }
  /**
   * Enable or disable the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setEnabled(false);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   *
   * @since 2.0.0
   */
  async setEnabled(e) {
    return s("plugin:window|set_enabled", {
      label: this.label,
      value: e
    });
  }
  /**
   * Whether the window is enabled or disabled.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setEnabled(false);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   *
   * @since 2.0.0
   */
  async isEnabled() {
    return s("plugin:window|is_enabled", {
      label: this.label
    });
  }
  /**
   * Sets whether the window's native maximize button is enabled or not.
   * If resizable is set to false, this setting is ignored.
   *
   * #### Platform-specific
   *
   * - **macOS:** Disables the "zoom" button in the window titlebar, which is also used to enter fullscreen mode.
   * - **Linux / iOS / Android:** Unsupported.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setMaximizable(false);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async setMaximizable(e) {
    return s("plugin:window|set_maximizable", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets whether the window's native minimize button is enabled or not.
   *
   * #### Platform-specific
   *
   * - **Linux / iOS / Android:** Unsupported.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setMinimizable(false);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async setMinimizable(e) {
    return s("plugin:window|set_minimizable", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets whether the window's native close button is enabled or not.
   *
   * #### Platform-specific
   *
   * - **Linux:** GTK+ will do its best to convince the window manager not to show a close button. Depending on the system, this function may not have any effect when called on a window that is already visible
   * - **iOS / Android:** Unsupported.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setClosable(false);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async setClosable(e) {
    return s("plugin:window|set_closable", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets the window title.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setTitle('Tauri');
   * ```
   *
   * @param title The new title
   * @returns A promise indicating the success or failure of the operation.
   */
  async setTitle(e) {
    return s("plugin:window|set_title", {
      label: this.label,
      value: e
    });
  }
  /**
   * Maximizes the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().maximize();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async maximize() {
    return s("plugin:window|maximize", {
      label: this.label
    });
  }
  /**
   * Unmaximizes the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().unmaximize();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async unmaximize() {
    return s("plugin:window|unmaximize", {
      label: this.label
    });
  }
  /**
   * Toggles the window maximized state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().toggleMaximize();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async toggleMaximize() {
    return s("plugin:window|toggle_maximize", {
      label: this.label
    });
  }
  /**
   * Minimizes the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().minimize();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async minimize() {
    return s("plugin:window|minimize", {
      label: this.label
    });
  }
  /**
   * Unminimizes the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().unminimize();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async unminimize() {
    return s("plugin:window|unminimize", {
      label: this.label
    });
  }
  /**
   * Sets the window visibility to true.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().show();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async show() {
    return s("plugin:window|show", {
      label: this.label
    });
  }
  /**
   * Sets the window visibility to false.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().hide();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async hide() {
    return s("plugin:window|hide", {
      label: this.label
    });
  }
  /**
   * Closes the window.
   *
   * Note this emits a closeRequested event so you can intercept it. To force window close, use {@link Window.destroy}.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().close();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async close() {
    return s("plugin:window|close", {
      label: this.label
    });
  }
  /**
   * Destroys the window. Behaves like {@link Window.close} but forces the window close instead of emitting a closeRequested event.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().destroy();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async destroy() {
    return s("plugin:window|destroy", {
      label: this.label
    });
  }
  /**
   * Whether the window should have borders and bars.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setDecorations(false);
   * ```
   *
   * @param decorations Whether the window should have borders and bars.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setDecorations(e) {
    return s("plugin:window|set_decorations", {
      label: this.label,
      value: e
    });
  }
  /**
   * Whether or not the window should have shadow.
   *
   * #### Platform-specific
   *
   * - **Windows:**
   *   - `false` has no effect on decorated window, shadows are always ON.
   *   - `true` will make undecorated window have a 1px white border,
   * and on Windows 11, it will have a rounded corners.
   * - **Linux:** Unsupported.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setShadow(false);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async setShadow(e) {
    return s("plugin:window|set_shadow", {
      label: this.label,
      value: e
    });
  }
  /**
   * Set window effects.
   */
  async setEffects(e) {
    return s("plugin:window|set_effects", {
      label: this.label,
      value: e
    });
  }
  /**
   * Clear any applied effects if possible.
   */
  async clearEffects() {
    return s("plugin:window|set_effects", {
      label: this.label,
      value: null
    });
  }
  /**
   * Whether the window should always be on top of other windows.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setAlwaysOnTop(true);
   * ```
   *
   * @param alwaysOnTop Whether the window should always be on top of other windows or not.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setAlwaysOnTop(e) {
    return s("plugin:window|set_always_on_top", {
      label: this.label,
      value: e
    });
  }
  /**
   * Whether the window should always be below other windows.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setAlwaysOnBottom(true);
   * ```
   *
   * @param alwaysOnBottom Whether the window should always be below other windows or not.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setAlwaysOnBottom(e) {
    return s("plugin:window|set_always_on_bottom", {
      label: this.label,
      value: e
    });
  }
  /**
   * Prevents the window contents from being captured by other apps.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setContentProtected(true);
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async setContentProtected(e) {
    return s("plugin:window|set_content_protected", {
      label: this.label,
      value: e
    });
  }
  /**
   * Resizes the window with a new inner size.
   * @example
   * ```typescript
   * import { getCurrentWindow, LogicalSize } from '@tauri-apps/api/window';
   * await getCurrentWindow().setSize(new LogicalSize(600, 500));
   * ```
   *
   * @param size The logical or physical inner size.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setSize(e) {
    return s("plugin:window|set_size", {
      label: this.label,
      value: e instanceof ce ? e : new ce(e)
    });
  }
  /**
   * Sets the window minimum inner size. If the `size` argument is not provided, the constraint is unset.
   * @example
   * ```typescript
   * import { getCurrentWindow, PhysicalSize } from '@tauri-apps/api/window';
   * await getCurrentWindow().setMinSize(new PhysicalSize(600, 500));
   * ```
   *
   * @param size The logical or physical inner size, or `null` to unset the constraint.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setMinSize(e) {
    return s("plugin:window|set_min_size", {
      label: this.label,
      value: e instanceof ce ? e : e ? new ce(e) : null
    });
  }
  /**
   * Sets the window maximum inner size. If the `size` argument is undefined, the constraint is unset.
   * @example
   * ```typescript
   * import { getCurrentWindow, LogicalSize } from '@tauri-apps/api/window';
   * await getCurrentWindow().setMaxSize(new LogicalSize(600, 500));
   * ```
   *
   * @param size The logical or physical inner size, or `null` to unset the constraint.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setMaxSize(e) {
    return s("plugin:window|set_max_size", {
      label: this.label,
      value: e instanceof ce ? e : e ? new ce(e) : null
    });
  }
  /**
   * Sets the window inner size constraints.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setSizeConstraints({ minWidth: 300 });
   * ```
   *
   * @param constraints The logical or physical inner size, or `null` to unset the constraint.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setSizeConstraints(e) {
    function n(a) {
      return a ? { Logical: a } : null;
    }
    return s("plugin:window|set_size_constraints", {
      label: this.label,
      value: {
        minWidth: n(e?.minWidth),
        minHeight: n(e?.minHeight),
        maxWidth: n(e?.maxWidth),
        maxHeight: n(e?.maxHeight)
      }
    });
  }
  /**
   * Sets the window outer position.
   * @example
   * ```typescript
   * import { getCurrentWindow, LogicalPosition } from '@tauri-apps/api/window';
   * await getCurrentWindow().setPosition(new LogicalPosition(600, 500));
   * ```
   *
   * @param position The new position, in logical or physical pixels.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setPosition(e) {
    return s("plugin:window|set_position", {
      label: this.label,
      value: e instanceof xe ? e : new xe(e)
    });
  }
  /**
   * Sets the window fullscreen state.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setFullscreen(true);
   * ```
   *
   * @param fullscreen Whether the window should go to fullscreen or not.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setFullscreen(e) {
    return s("plugin:window|set_fullscreen", {
      label: this.label,
      value: e
    });
  }
  /**
   * Bring the window to front and focus.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setFocus();
   * ```
   *
   * @returns A promise indicating the success or failure of the operation.
   */
  async setFocus() {
    return s("plugin:window|set_focus", {
      label: this.label
    });
  }
  /**
   * Sets the window icon.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setIcon('/tauri/awesome.png');
   * ```
   *
   * Note that you may need the `image-ico` or `image-png` Cargo features to use this API.
   * To enable it, change your Cargo.toml file:
   * ```toml
   * [dependencies]
   * tauri = { version = "...", features = ["...", "image-png"] }
   * ```
   *
   * @param icon Icon bytes or path to the icon file.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setIcon(e) {
    return s("plugin:window|set_icon", {
      label: this.label,
      value: Pe(e)
    });
  }
  /**
   * Whether the window icon should be hidden from the taskbar or not.
   *
   * #### Platform-specific
   *
   * - **macOS:** Unsupported.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setSkipTaskbar(true);
   * ```
   *
   * @param skip true to hide window icon, false to show it.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setSkipTaskbar(e) {
    return s("plugin:window|set_skip_taskbar", {
      label: this.label,
      value: e
    });
  }
  /**
   * Grabs the cursor, preventing it from leaving the window.
   *
   * There's no guarantee that the cursor will be hidden. You should
   * hide it by yourself if you want so.
   *
   * #### Platform-specific
   *
   * - **Linux:** Unsupported.
   * - **macOS:** This locks the cursor in a fixed location, which looks visually awkward.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setCursorGrab(true);
   * ```
   *
   * @param grab `true` to grab the cursor icon, `false` to release it.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setCursorGrab(e) {
    return s("plugin:window|set_cursor_grab", {
      label: this.label,
      value: e
    });
  }
  /**
   * Modifies the cursor's visibility.
   *
   * #### Platform-specific
   *
   * - **Windows:** The cursor is only hidden within the confines of the window.
   * - **macOS:** The cursor is hidden as long as the window has input focus, even if the cursor is
   *   outside of the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setCursorVisible(false);
   * ```
   *
   * @param visible If `false`, this will hide the cursor. If `true`, this will show the cursor.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setCursorVisible(e) {
    return s("plugin:window|set_cursor_visible", {
      label: this.label,
      value: e
    });
  }
  /**
   * Modifies the cursor icon of the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setCursorIcon('help');
   * ```
   *
   * @param icon The new cursor icon.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setCursorIcon(e) {
    return s("plugin:window|set_cursor_icon", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets the window background color.
   *
   * #### Platform-specific:
   *
   * - **Windows:** alpha channel is ignored.
   * - **iOS / Android:** Unsupported.
   *
   * @returns A promise indicating the success or failure of the operation.
   *
   * @since 2.1.0
   */
  async setBackgroundColor(e) {
    return s("plugin:window|set_background_color", { color: e });
  }
  /**
   * Changes the position of the cursor in window coordinates.
   * @example
   * ```typescript
   * import { getCurrentWindow, LogicalPosition } from '@tauri-apps/api/window';
   * await getCurrentWindow().setCursorPosition(new LogicalPosition(600, 300));
   * ```
   *
   * @param position The new cursor position.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setCursorPosition(e) {
    return s("plugin:window|set_cursor_position", {
      label: this.label,
      value: e instanceof xe ? e : new xe(e)
    });
  }
  /**
   * Changes the cursor events behavior.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setIgnoreCursorEvents(true);
   * ```
   *
   * @param ignore `true` to ignore the cursor events; `false` to process them as usual.
   * @returns A promise indicating the success or failure of the operation.
   */
  async setIgnoreCursorEvents(e) {
    return s("plugin:window|set_ignore_cursor_events", {
      label: this.label,
      value: e
    });
  }
  /**
   * Starts dragging the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().startDragging();
   * ```
   *
   * @return A promise indicating the success or failure of the operation.
   */
  async startDragging() {
    return s("plugin:window|start_dragging", {
      label: this.label
    });
  }
  /**
   * Starts resize-dragging the window.
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().startResizeDragging();
   * ```
   *
   * @return A promise indicating the success or failure of the operation.
   */
  async startResizeDragging(e) {
    return s("plugin:window|start_resize_dragging", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets the badge count. It is app wide and not specific to this window.
   *
   * #### Platform-specific
   *
   * - **Windows**: Unsupported. Use @{linkcode Window.setOverlayIcon} instead.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setBadgeCount(5);
   * ```
   *
   * @param count The badge count. Use `undefined` to remove the badge.
   * @return A promise indicating the success or failure of the operation.
   */
  async setBadgeCount(e) {
    return s("plugin:window|set_badge_count", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets the badge cont **macOS only**.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setBadgeLabel("Hello");
   * ```
   *
   * @param label The badge label. Use `undefined` to remove the badge.
   * @return A promise indicating the success or failure of the operation.
   */
  async setBadgeLabel(e) {
    return s("plugin:window|set_badge_label", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets the overlay icon. **Windows only**
   * The overlay icon can be set for every window.
   *
   *
   * Note that you may need the `image-ico` or `image-png` Cargo features to use this API.
   * To enable it, change your Cargo.toml file:
   *
   * ```toml
   * [dependencies]
   * tauri = { version = "...", features = ["...", "image-png"] }
   * ```
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from '@tauri-apps/api/window';
   * await getCurrentWindow().setOverlayIcon("/tauri/awesome.png");
   * ```
   *
   * @param icon Icon bytes or path to the icon file. Use `undefined` to remove the overlay icon.
   * @return A promise indicating the success or failure of the operation.
   */
  async setOverlayIcon(e) {
    return s("plugin:window|set_overlay_icon", {
      label: this.label,
      value: e ? Pe(e) : void 0
    });
  }
  /**
   * Sets the taskbar progress state.
   *
   * #### Platform-specific
   *
   * - **Linux / macOS**: Progress bar is app-wide and not specific to this window.
   * - **Linux**: Only supported desktop environments with `libunity` (e.g. GNOME).
   *
   * @example
   * ```typescript
   * import { getCurrentWindow, ProgressBarStatus } from '@tauri-apps/api/window';
   * await getCurrentWindow().setProgressBar({
   *   status: ProgressBarStatus.Normal,
   *   progress: 50,
   * });
   * ```
   *
   * @return A promise indicating the success or failure of the operation.
   */
  async setProgressBar(e) {
    return s("plugin:window|set_progress_bar", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets whether the window should be visible on all workspaces or virtual desktops.
   *
   * #### Platform-specific
   *
   * - **Windows / iOS / Android:** Unsupported.
   *
   * @since 2.0.0
   */
  async setVisibleOnAllWorkspaces(e) {
    return s("plugin:window|set_visible_on_all_workspaces", {
      label: this.label,
      value: e
    });
  }
  /**
   * Sets the title bar style. **macOS only**.
   *
   * @since 2.0.0
   */
  async setTitleBarStyle(e) {
    return s("plugin:window|set_title_bar_style", {
      label: this.label,
      value: e
    });
  }
  /**
   * Set window theme, pass in `null` or `undefined` to follow system theme
   *
   * #### Platform-specific
   *
   * - **Linux / macOS**: Theme is app-wide and not specific to this window.
   * - **iOS / Android:** Unsupported.
   *
   * @since 2.0.0
   */
  async setTheme(e) {
    return s("plugin:window|set_theme", {
      label: this.label,
      value: e
    });
  }
  // Listeners
  /**
   * Listen to window resize.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from "@tauri-apps/api/window";
   * const unlisten = await getCurrentWindow().onResized(({ payload: size }) => {
   *  console.log('Window resized', size);
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async onResized(e) {
    return this.listen(R.WINDOW_RESIZED, (n) => {
      n.payload = new we(n.payload), e(n);
    });
  }
  /**
   * Listen to window move.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from "@tauri-apps/api/window";
   * const unlisten = await getCurrentWindow().onMoved(({ payload: position }) => {
   *  console.log('Window moved', position);
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async onMoved(e) {
    return this.listen(R.WINDOW_MOVED, (n) => {
      n.payload = new X(n.payload), e(n);
    });
  }
  /**
   * Listen to window close requested. Emitted when the user requests to closes the window.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from "@tauri-apps/api/window";
   * import { confirm } from '@tauri-apps/api/dialog';
   * const unlisten = await getCurrentWindow().onCloseRequested(async (event) => {
   *   const confirmed = await confirm('Are you sure?');
   *   if (!confirmed) {
   *     // user did not confirm closing the window; let's prevent it
   *     event.preventDefault();
   *   }
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async onCloseRequested(e) {
    return this.listen(R.WINDOW_CLOSE_REQUESTED, async (n) => {
      const a = new An(n);
      await e(a), a.isPreventDefault() || await this.destroy();
    });
  }
  /**
   * Listen to a file drop event.
   * The listener is triggered when the user hovers the selected files on the webview,
   * drops the files or cancels the operation.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from "@tauri-apps/api/webview";
   * const unlisten = await getCurrentWindow().onDragDropEvent((event) => {
   *  if (event.payload.type === 'over') {
   *    console.log('User hovering', event.payload.position);
   *  } else if (event.payload.type === 'drop') {
   *    console.log('User dropped', event.payload.paths);
   *  } else {
   *    console.log('File drop cancelled');
   *  }
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async onDragDropEvent(e) {
    const n = await this.listen(R.DRAG_ENTER, (u) => {
      e({
        ...u,
        payload: {
          type: "enter",
          paths: u.payload.paths,
          position: new X(u.payload.position)
        }
      });
    }), a = await this.listen(R.DRAG_OVER, (u) => {
      e({
        ...u,
        payload: {
          type: "over",
          position: new X(u.payload.position)
        }
      });
    }), r = await this.listen(R.DRAG_DROP, (u) => {
      e({
        ...u,
        payload: {
          type: "drop",
          paths: u.payload.paths,
          position: new X(u.payload.position)
        }
      });
    }), c = await this.listen(R.DRAG_LEAVE, (u) => {
      e({ ...u, payload: { type: "leave" } });
    });
    return () => {
      n(), r(), a(), c();
    };
  }
  /**
   * Listen to window focus change.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from "@tauri-apps/api/window";
   * const unlisten = await getCurrentWindow().onFocusChanged(({ payload: focused }) => {
   *  console.log('Focus changed, window is focused? ' + focused);
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async onFocusChanged(e) {
    const n = await this.listen(R.WINDOW_FOCUS, (r) => {
      e({ ...r, payload: !0 });
    }), a = await this.listen(R.WINDOW_BLUR, (r) => {
      e({ ...r, payload: !1 });
    });
    return () => {
      n(), a();
    };
  }
  /**
   * Listen to window scale change. Emitted when the window's scale factor has changed.
   * The following user actions can cause DPI changes:
   * - Changing the display's resolution.
   * - Changing the display's scale factor (e.g. in Control Panel on Windows).
   * - Moving the window to a display with a different scale factor.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from "@tauri-apps/api/window";
   * const unlisten = await getCurrentWindow().onScaleChanged(({ payload }) => {
   *  console.log('Scale changed', payload.scaleFactor, payload.size);
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async onScaleChanged(e) {
    return this.listen(R.WINDOW_SCALE_FACTOR_CHANGED, e);
  }
  /**
   * Listen to the system theme change.
   *
   * @example
   * ```typescript
   * import { getCurrentWindow } from "@tauri-apps/api/window";
   * const unlisten = await getCurrentWindow().onThemeChanged(({ payload: theme }) => {
   *  console.log('New theme: ' + theme);
   * });
   *
   * // you need to call unlisten if your handler goes out of scope e.g. the component is unmounted
   * unlisten();
   * ```
   *
   * @returns A promise resolving to a function to unlisten to the event.
   * Note that removing the listener is required if your listener goes out of scope e.g. the component is unmounted.
   */
  async onThemeChanged(e) {
    return this.listen(R.WINDOW_THEME_CHANGED, e);
  }
}
var ct;
(function(t) {
  t.Disabled = "disabled", t.Throttle = "throttle", t.Suspend = "suspend";
})(ct || (ct = {}));
var dt;
(function(t) {
  t.AppearanceBased = "appearanceBased", t.Light = "light", t.Dark = "dark", t.MediumLight = "mediumLight", t.UltraDark = "ultraDark", t.Titlebar = "titlebar", t.Selection = "selection", t.Menu = "menu", t.Popover = "popover", t.Sidebar = "sidebar", t.HeaderView = "headerView", t.Sheet = "sheet", t.WindowBackground = "windowBackground", t.HudWindow = "hudWindow", t.FullScreenUI = "fullScreenUI", t.Tooltip = "tooltip", t.ContentBackground = "contentBackground", t.UnderWindowBackground = "underWindowBackground", t.UnderPageBackground = "underPageBackground", t.Mica = "mica", t.Blur = "blur", t.Acrylic = "acrylic", t.Tabbed = "tabbed", t.TabbedDark = "tabbedDark", t.TabbedLight = "tabbedLight";
})(dt || (dt = {}));
var ht;
(function(t) {
  t.FollowsWindowActiveState = "followsWindowActiveState", t.Active = "active", t.Inactive = "inactive";
})(ht || (ht = {}));
const Rn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%201.1//EN'%20'http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd'%3e%3csvg%20t='1680942651462'%20class='icon'%20viewBox='0%200%201024%201024'%20version='1.1'%20xmlns='http://www.w3.org/2000/svg'%20p-id='3789'%20data-darkreader-inline-fill=''%20xmlns:xlink='http://www.w3.org/1999/xlink'%20width='200'%20height='200'%3e%3cpath%20d='M214.101333%20512c0-32.512%205.546667-63.701333%2015.36-92.928L57.173333%20290.218667A491.861333%20491.861333%200%200%200%204.693333%20512c0%2079.701333%2018.858667%20154.88%2052.394667%20221.610667l172.202667-129.066667A290.56%20290.56%200%200%201%20214.101333%20512'%20fill='%23FBBC05'%20p-id='3790'%20data-darkreader-inline-fill=''%20style='--darkreader-inline-fill:%20%23967002;'%3e%3c/path%3e%3cpath%20d='M516.693333%20216.192c72.106667%200%20137.258667%2025.002667%20188.458667%2065.962667L854.101333%20136.533333C763.349333%2059.178667%20646.997333%2011.392%20516.693333%2011.392c-202.325333%200-376.234667%20113.28-459.52%20278.826667l172.373334%20128.853333c39.68-118.016%20152.832-202.88%20287.146666-202.88'%20fill='%23EA4335'%20p-id='3791'%20data-darkreader-inline-fill=''%20style='--darkreader-inline-fill:%20%23a71d11;'%3e%3c/path%3e%3cpath%20d='M516.693333%20807.808c-134.357333%200-247.509333-84.864-287.232-202.88l-172.288%20128.853333c83.242667%20165.546667%20257.152%20278.826667%20459.52%20278.826667%20124.842667%200%20244.053333-43.392%20333.568-124.757333l-163.584-123.818667c-46.122667%2028.458667-104.234667%2043.776-170.026666%2043.776'%20fill='%2334A853'%20p-id='3792'%20data-darkreader-inline-fill=''%20style='--darkreader-inline-fill:%20%232a8642;'%3e%3c/path%3e%3cpath%20d='M1005.397333%20512c0-29.568-4.693333-61.44-11.648-91.008H516.650667V614.4h274.602666c-13.696%2065.962667-51.072%20116.650667-104.533333%20149.632l163.541333%20123.818667c93.994667-85.418667%20155.136-212.650667%20155.136-375.850667'%20fill='%234285F4'%20p-id='3793'%20data-darkreader-inline-fill=''%20style='--darkreader-inline-fill:%20%230943a2;'%3e%3c/path%3e%3c/svg%3e", $n = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2048%2048'%20width='240px'%20height='240px'%3e%3clinearGradient%20id='ZkmZ8eVihrQTUnr9TZpNla'%20x1='11.905'%20x2='17.941'%20y1='1.952'%20y2='40.401'%20gradientUnits='userSpaceOnUse'%3e%3cstop%20offset='0'%20stop-color='%233dbffc'/%3e%3cstop%20offset='1'%20stop-color='%23183efb'/%3e%3c/linearGradient%3e%3cpath%20fill='url(%23ZkmZ8eVihrQTUnr9TZpNla)'%20d='M17.572,37.076L20,35.619V10.603c0-1.632-0.796-3.161-2.133-4.096L12.36,2.652%20C11.366,1.956,10,2.667,10,3.881V32.5c0,0.22,0.02,0.555,0.033,0.772C10.369,36.867,14.382,38.99,17.572,37.076z'/%3e%3clinearGradient%20id='ZkmZ8eVihrQTUnr9TZpNlb'%20x1='14.342'%20x2='34.121'%20y1='41.478'%20y2='25.575'%20gradientUnits='userSpaceOnUse'%3e%3cstop%20offset='0'%20stop-color='%2333bef0'/%3e%3cstop%20offset='.159'%20stop-color='%2332b9f0'/%3e%3cstop%20offset='.341'%20stop-color='%232facf2'/%3e%3cstop%20offset='.533'%20stop-color='%232a95f4'/%3e%3cstop%20offset='.733'%20stop-color='%232475f6'/%3e%3cstop%20offset='.936'%20stop-color='%231b4cfa'/%3e%3cstop%20offset='1'%20stop-color='%23183efb'/%3e%3c/linearGradient%3e%3cpath%20fill='url(%23ZkmZ8eVihrQTUnr9TZpNlb)'%20d='M32.682,27.904L20,35.5v0l-2.428,1.457c-3.191,1.915-7.203-0.209-7.54-3.804%20C10.372,38.922,15.145,43.5,21,43.5c1.963,0,3.888-0.536,5.568-1.551l6.834-4.126c0.817-0.493,1.522-1.075,2.15-1.707%20C37.906,33.415,36.739,28.669,32.682,27.904z'/%3e%3clinearGradient%20id='ZkmZ8eVihrQTUnr9TZpNlc'%20x1='24.223'%20x2='45.699'%20y1='17.113'%20y2='38.588'%20gradientUnits='userSpaceOnUse'%3e%3cstop%20offset='0'%20stop-color='%233dbffd'/%3e%3cstop%20offset='1'%20stop-color='%231de9b6'/%3e%3c/linearGradient%3e%3cpath%20fill='url(%23ZkmZ8eVihrQTUnr9TZpNlc)'%20d='M33.636,19.568l-7.607-3.803c-1.234-0.617-2.576,0.618-2.064,1.899l1.755,5.886%20c0.499,1.248,1.479,2.242,2.719,2.758L32.5,28c4.057,0.766,5.352,5.251,3.052,8.117C40.399,31.24,40.088,22.794,33.636,19.568z'/%3e%3c/svg%3e";
function Hn(t) {
  return t.replace(/([a-z])([A-Z])/g, "$1 $2").replace(/([A-Z]+)([A-Z][a-z])/g, "$1 $2");
}
function Re(t) {
  if (!t) return "unknown";
  const e = t.match(/[\u4e00-\u9fa5]/g), n = e ? e.length : 0, a = t.match(/[\u3040-\u30ff]/g), r = a ? a.length : 0, c = t.match(/[\uac00-\ud7af]/g), u = c ? c.length : 0, w = t.match(/[a-zA-Z]/g), v = w ? w.length : 0, _ = n + r + u + v;
  return _ === 0 ? "unknown" : r >= 3 && r > v && r / _ >= 0.2 ? "ja" : u >= 3 && u > n && u > v && u / _ >= 0.2 ? "ko" : n > v ? "zh" : v > n ? "en" : "unknown";
}
const Fn = { class: "translate-window" }, Un = {
  class: "header",
  "data-tauri-drag-region": ""
}, Zn = { class: "left-buttons" }, Gn = {
  class: "bg-penal text-base",
  "data-tauri-drag-region": ""
}, Kn = { class: "right-buttons" }, qn = { class: "translate-container transparent-input" }, Jn = { class: "language-selector" }, Qn = { class: "source-area" }, Yn = { class: "source-actions" }, Xn = { class: "source-material" }, ea = { class: "translation-results" }, ta = ["onClick"], na = { class: "result-title" }, aa = ["src"], la = ["src"], ia = {
  key: 2,
  class: "offline-icon"
}, sa = { class: "result-controls" }, oa = {
  key: 0,
  class: "result-body"
}, ra = {
  key: 1,
  class: "result-text"
}, ua = {
  key: 2,
  class: "result-empty"
}, ca = { class: "result-actions" }, da = ["onClick"], ha = ["onClick"], fa = ["onClick"], pa = /* @__PURE__ */ oe({
  __name: "index",
  setup(t) {
    const { t: e } = Jt(), n = yt(), a = N(!1), r = N(""), c = N("auto"), u = N("zh"), w = N(!1), v = N(!1), E = N(), _ = N(!1), x = N([
      {
        engine: "bing",
        name: "",
        text: "",
        loading: !1,
        expanded: !0
      },
      {
        engine: "google",
        name: "",
        text: "",
        loading: !1,
        expanded: !0
      },
      {
        engine: "offline",
        name: "",
        text: "",
        loading: !1,
        expanded: !0
      }
    ]), G = z(() => x.value.filter((o) => o.engine === "offline" ? _.value : !0)), H = (o) => o === "bing" ? e("translate.bingTranslate") : o === "google" ? e("translate.googleTranslate") : o === "offline" ? e("translate.offlineTranslate") : o, p = z(() => [
      { value: "auto", label: e("translate.languages.auto") },
      { value: "zh", label: e("translate.languages.zh") },
      { value: "zh_tw", label: e("translate.languages.zh_tw") },
      { value: "en", label: e("translate.languages.en") },
      { value: "ja", label: e("translate.languages.ja") },
      { value: "ko", label: e("translate.languages.ko") },
      { value: "fr", label: e("translate.languages.fr") },
      { value: "de", label: e("translate.languages.de") },
      { value: "ru", label: e("translate.languages.ru") },
      { value: "es", label: e("translate.languages.es") },
      { value: "pt_pt", label: e("translate.languages.pt_pt") },
      { value: "pt_br", label: e("translate.languages.pt_br") },
      { value: "vi", label: e("translate.languages.vi") },
      { value: "id", label: e("translate.languages.id") },
      { value: "th", label: e("translate.languages.th") },
      { value: "ar", label: e("translate.languages.ar") }
    ]);
    let D = null, j = null, P = null;
    const $ = N({
      forChinese: "en",
      forEnglish: "zh"
    }), ne = async () => {
      if (w.value) return;
      const o = await ie("tauri://blur", () => {
        D && clearTimeout(D), D = setTimeout(async () => {
          w.value || (F(), await n.hide());
        }, 100);
      }), f = await ie("tauri://focus", () => {
        D && clearTimeout(D), Y();
      }), T = await ie("tauri://show", () => {
        Y();
      }), S = await ie("tauri://move", () => {
        D && clearTimeout(D);
      }), M = await ie("selection-text", (W) => {
        W.payload && W.payload.text && he(W.payload.text);
      }), C = await ie("reset-state", () => {
        F();
      });
      j = () => {
        o(), f(), S(), M(), C(), T();
      };
    }, ye = async () => {
      w.value = !w.value, await n.setAlwaysOnTop(w.value), w.value ? j && (j(), j = null) : ne();
    }, F = () => {
      r.value = "", v.value = !1, x.value.forEach((o) => {
        o.text = "", o.loading = !1;
      });
    }, K = async () => {
      F(), await n.close();
    }, Ve = () => {
      c.value !== "auto" && ([c.value, u.value] = [
        u.value,
        c.value
      ], q());
    }, re = () => {
      r.value = "", x.value.forEach((o) => {
        o.text = "";
      }), v.value = !1;
    }, Ne = async (o) => {
      o && (await navigator.clipboard.writeText(o), tn.success(e("translate.copiedToClipboard")));
    }, me = (o, f) => {
      if (!o) return;
      const T = new SpeechSynthesisUtterance(o), S = f === "zh" ? "zh-CN" : f === "zh_tw" ? "zh-TW" : f === "en" ? "en-US" : f;
      T.lang = S, window.speechSynthesis.speak(T);
    }, ae = () => {
      if (c.value !== "auto") return;
      const o = Re(r.value);
      o === "zh" ? u.value = $.value.forChinese : o === "en" && (u.value = $.value.forEnglish);
    }, le = () => {
      const o = Re(r.value);
      o === "zh" ? $.value.forChinese = u.value : o === "en" && ($.value.forEnglish = u.value), q();
    }, ke = async (o) => {
      const f = x.value.find((M) => M.engine === o);
      if (!f || o === "offline" && !_.value)
        return;
      f.loading = !0, f.text = "";
      const T = Hn(r.value);
      let S = u.value;
      o === "bing" && S === "auto" && (S = "en");
      try {
        let M;
        if (o === "offline") {
          const { canUseOfflineTranslation: C, isModelCached: W, warmupOfflineTranslator: g } = await import("./_plugin-vue_export-helper-C3e6AB_k.js").then((U) => U.a6);
          if (!C())
            if (await W())
              pe.info("[翻译窗口] 离线翻译懒加载：开始加载模型..."), await g(), pe.info("[翻译窗口] 离线翻译懒加载：模型加载完成");
            else
              throw new Error("离线翻译模型未下载，请在设置中下载模型");
          M = await Xt(T);
        } else
          M = await s("translate_text", {
            text: T,
            from: c.value,
            to: S,
            engine: o
          });
        f.text = M;
      } catch (M) {
        pe.error(`[翻译] ${o}翻译出错`, M);
        const C = String(M);
        C.includes("429") || C.includes("Too Many Requests") ? f.text = e("translate.tooManyRequests") : C.includes("timeout") || C.includes("超时") ? f.text = e("translate.timeout") : C.includes("network") || C.includes("网络") ? f.text = e("translate.networkError") : C.includes("未下载") || C.includes("未激活") || C.includes("运行时未安装") ? f.text = C : f.text = e("translate.translateFailed");
      } finally {
        f.loading = !1;
      }
    }, q = async () => {
      if (!r.value.trim()) {
        x.value.forEach((f) => {
          f.text = "";
        });
        return;
      }
      ae(), a.value = !0;
      const o = G.value.map(
        (f) => ke(f.engine)
      );
      await Promise.all(o), a.value = !1;
    }, Me = (o) => {
      o.expanded = !o.expanded;
    }, _e = () => {
      v.value = !!r.value, P && clearTimeout(P), r.value.trim() ? P = setTimeout(() => {
        q();
      }, 500) : x.value.forEach((o) => {
        o.text = "";
      });
    };
    let A = "", J = !1;
    const he = (o) => {
      o && (o === A && J || (A = o, J = !0, r.value = o, v.value = !0, ae(), q().finally(() => {
        setTimeout(() => {
          J = !1;
        }, 2e3);
      }), Y()));
    }, Q = (o) => {
      r.value = o.text;
      const f = Re(r.value), T = c.value, S = u.value;
      T === "auto" ? (c.value = "auto", f === "zh" ? u.value = $.value.forChinese : u.value = $.value.forEnglish) : (c.value = S, u.value = T === "auto" ? f === "zh" ? "en" : "zh" : T), q();
    }, Y = () => {
      const o = (f = 0) => {
        if (!(f > 5))
          if (E.value)
            E.value.focus();
          else {
            const T = 100 * (f + 1);
            setTimeout(() => o(f + 1), T);
          }
      };
      E.value && E.value.focus(), se(() => {
        o();
      });
    }, We = () => {
      ae(), q();
    };
    return Ge(async () => {
      try {
        const o = await s("get_offline_model_activated");
        pe.info(`[翻译窗口] 离线模型后端激活状态: ${o}`), _.value = o;
      } catch (o) {
        pe.error("[翻译窗口] 获取离线模型激活状态失败:", o), _.value = !1;
      }
      await ne(), r.value.trim() && ae(), Y(), await se(), setTimeout(() => {
        n.emit("translate_ready");
      }, 100);
    }), cn(() => {
      j && j(), D && clearTimeout(D), P && clearTimeout(P), F();
    }), (o, f) => {
      const T = en, S = Qt, M = En, C = Yt, W = jn;
      return h(), m("main", Fn, [
        b("div", Un, [
          b("div", Zn, [
            b(
              "div",
              {
                onClick: ye,
                class: L(["pin-button", l(w) ? "pinned" : ""])
              },
              [
                d(l(gn), { size: 18 })
              ],
              2
              /* CLASS */
            )
          ]),
          b(
            "div",
            Gn,
            ee(o.$t("translate.title")),
            1
            /* TEXT */
          ),
          b("div", Kn, [
            b("div", {
              onClick: K,
              class: "material-close"
            }, [
              d(l(dn), { size: 22 })
            ])
          ])
        ]),
        b("div", qn, [
          k(" 语言选择区域 "),
          b("div", Jn, [
            d(S, {
              modelValue: l(c),
              "onUpdate:modelValue": f[0] || (f[0] = (g) => Be(c) ? c.value = g : null),
              size: "small",
              onChange: We,
              class: "lang-select"
            }, {
              default: O(() => [
                (h(!0), m(
                  Z,
                  null,
                  ve(l(p), (g) => (h(), I(T, {
                    key: g.value,
                    label: g.label,
                    value: g.value
                  }, null, 8, ["label", "value"]))),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ]),
              _: 1
              /* STABLE */
            }, 8, ["modelValue"]),
            b("div", {
              size: "small",
              onClick: Ve,
              class: "swap-button"
            }, [
              d(l(st), { size: 22 })
            ]),
            d(S, {
              modelValue: l(u),
              "onUpdate:modelValue": f[1] || (f[1] = (g) => Be(u) ? u.value = g : null),
              size: "small",
              onChange: le,
              class: "lang-select"
            }, {
              default: O(() => [
                (h(!0), m(
                  Z,
                  null,
                  ve(l(p), (g) => (h(), I(T, {
                    key: g.value,
                    label: g.label,
                    value: g.value
                  }, null, 8, ["label", "value"]))),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ]),
              _: 1
              /* STABLE */
            }, 8, ["modelValue"])
          ]),
          k(" 源文本输入区域 "),
          b("div", Qn, [
            d(M, {
              ref_key: "sourceTextArea",
              ref: E,
              modelValue: l(r),
              "onUpdate:modelValue": f[2] || (f[2] = (g) => Be(r) ? r.value = g : null),
              type: "textarea",
              rows: 2,
              placeholder: o.$t("translate.inputPlaceholder"),
              resize: "none",
              onInput: _e,
              class: "source-textarea"
            }, null, 8, ["modelValue", "placeholder"]),
            b("div", Yn, [
              b("div", Xn, [
                d(C, {
                  content: o.$t("translate.speakText"),
                  placement: "top",
                  "hide-after": 1e3
                }, {
                  default: O(() => [
                    b("div", {
                      onClick: f[3] || (f[3] = (g) => me(l(r), l(c))),
                      class: "action-btn"
                    }, [
                      d(l(ot), { size: 18 })
                    ])
                  ]),
                  _: 1
                  /* STABLE */
                }, 8, ["content"]),
                l(v) ? (h(), I(C, {
                  key: 0,
                  content: o.$t("translate.deleteText"),
                  placement: "top",
                  "hide-after": 1e3
                }, {
                  default: O(() => [
                    b("div", {
                      onClick: re,
                      class: "action-btn"
                    }, [
                      d(l(fn), { size: 18 })
                    ])
                  ]),
                  _: 1
                  /* STABLE */
                }, 8, ["content"])) : k("v-if", !0)
              ])
            ])
          ]),
          k(" 多引擎翻译结果区域 "),
          b("div", ea, [
            (h(!0), m(
              Z,
              null,
              ve(l(G), (g) => (h(), m(
                "div",
                {
                  key: g.engine,
                  class: L(["result-card", { "result-expanded": g.expanded }])
                },
                [
                  b("div", {
                    class: "result-header",
                    onClick: (U) => Me(g)
                  }, [
                    b("div", na, [
                      g.engine === "google" ? (h(), m("img", {
                        key: 0,
                        src: l(Rn),
                        class: "engine-icon",
                        alt: "Google"
                      }, null, 8, aa)) : g.engine === "bing" ? (h(), m("img", {
                        key: 1,
                        src: l($n),
                        class: "engine-icon",
                        alt: "Bing"
                      }, null, 8, la)) : (h(), m("span", ia, "离")),
                      b(
                        "span",
                        null,
                        ee(H(g.engine)),
                        1
                        /* TEXT */
                      )
                    ]),
                    b("div", sa, [
                      (h(), I(ge(g.expanded ? l(vn) : l(pn)), {
                        size: 18,
                        class: "expand-icon"
                      }))
                    ])
                  ], 8, ta),
                  g.expanded ? (h(), m("div", oa, [
                    g.loading ? (h(), I(W, {
                      key: 0,
                      rows: 3,
                      animated: ""
                    })) : g.text ? (h(), m(
                      "div",
                      ra,
                      ee(g.text),
                      1
                      /* TEXT */
                    )) : (h(), m(
                      "div",
                      ua,
                      ee(o.$t("translate.resultPlaceholder")),
                      1
                      /* TEXT */
                    )),
                    b("div", ca, [
                      d(C, {
                        content: o.$t("translate.speakText"),
                        placement: "top",
                        "hide-after": 1e3
                      }, {
                        default: O(() => [
                          b("div", {
                            onClick: (U) => me(g.text, l(u)),
                            class: "action-btn"
                          }, [
                            d(l(ot), { size: 18 })
                          ], 8, da)
                        ]),
                        _: 2
                        /* DYNAMIC */
                      }, 1032, ["content"]),
                      d(C, {
                        content: o.$t("translate.copyResult"),
                        placement: "top",
                        "hide-after": 1e3
                      }, {
                        default: O(() => [
                          b("div", {
                            onClick: (U) => Ne(g.text),
                            class: "action-btn"
                          }, [
                            d(l(hn), { size: 18 })
                          ], 8, ha)
                        ]),
                        _: 2
                        /* DYNAMIC */
                      }, 1032, ["content"]),
                      d(C, {
                        content: o.$t("translate.translateBack"),
                        placement: "top",
                        "hide-after": 1e3
                      }, {
                        default: O(() => [
                          b("div", {
                            onClick: (U) => Q(g),
                            class: "action-btn rotate-icon"
                          }, [
                            d(l(st), { size: 18 })
                          ], 8, fa)
                        ]),
                        _: 2
                        /* DYNAMIC */
                      }, 1032, ["content"])
                    ])
                  ])) : k("v-if", !0)
                ],
                2
                /* CLASS */
              ))),
              128
              /* KEYED_FRAGMENT */
            ))
          ])
        ])
      ]);
    };
  }
}), wa = /* @__PURE__ */ nn(pa, [["__scopeId", "data-v-82ca46e5"]]);
export {
  wa as default
};
