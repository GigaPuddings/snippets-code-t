🐛 问题修复：

- 修复应用与书签启动器安装后长期停在“解压中”、应用图标未生成及浏览器书签为空的问题；书签文本索引不再被浏览器 favicon 数据库阻塞，半完成索引会在后台自动补齐。
- 修复浏览器正在写入 favicon 数据库时后台备份无限重试，导致图标进度、插件页面和托盘长期停留在索引中的问题；本地快照读取现在有明确的失败边界并保证任务进入完成态。
- 修复禁用应用与书签启动器后设置路由、既有搜索结果和结果标签仍残留的问题；禁用会同步取消后台任务并立即卸载相关功能。
- 修复“重置软件”、清除使用历史和清理图标缓存之间的级联边界：索引重置改为事务执行，历史清理立即刷新排序，图标清理保留用户自定义图标且只为已启用插件后台补齐。
- 修复多个插件同时安装时进度互相覆盖、切换页面后状态丢失的问题；安装任务按包独立记录并串行落盘，重新打开配置窗口可恢复真实进度。

🔧 优化改进：

- 本地索引进度增加任务归属和阶段标识，页面与通知先监听再恢复快照；应用/书签列表改为非阻塞状态条和骨架屏，已有数据可在后台补图标时继续使用。
