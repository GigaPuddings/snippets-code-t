🎉 新增功能：

- 截图智能吸附新增父子元素层级选择：默认优先定位鼠标下较小的候选元素，可使用鼠标滚轮或 `Tab` 向父级切换，使用 `Shift + Tab` 返回子级，并显示当前候选层级。

🐛 问题修复：

- 修复截图吸附在鼠标停止移动后丢失较小候选，并自动跳转到更大父元素的问题；手动选定层级后不再被延迟识别结果覆盖。
- 修复浏览器历史记录、同步元数据等非书签内容变化被误判为书签变化，导致应用重启后重复执行本地启动器索引任务的问题。
- 修复应用启动和后台增量索引完成后仍弹出“数据索引完成”系统通知的问题；完成通知现在仅用于用户主动触发的强制重建。
- 修复 Windows 更新安装器启动失败时应用仍直接退出、用户无法获知失败原因的问题。
- 修复异常下载内容可能被当作 Windows 更新安装包写入缓存并继续尝试安装的问题。
- 修复发布构建或资产上传失败时，GitHub Latest Release 可能提前暴露空版本或不完整更新文件的问题。

🔧 优化改进：

- Windows 更新安装器改用系统 Shell 直接启动，并增加 EXE、MSI、ZIP 文件头校验及失败状态恢复。
- 更新发布流程改为先构建、以草稿上传全部安装资产和 `latest.json`，校验版本标签与安装包大小后再正式发布。
- 统一更新页、配置页及官方插件中的按钮实现，移除产品源码内直接使用的 Element Plus `el-button`，并增强 `CustomButton` 的图标、文本、链接、加载和原生按钮类型支持。
- 浏览器书签索引改用可搜索内容语义指纹，忽略 Chromium 非索引元数据及 Firefox 纯历史记录变化，同时保留文件状态缓存以减少重复解析。
- 官方插件运行时增加发布压缩，插件市场同步记录远程归档的真实 SHA-256 和下载大小。
- 补充截图元素层级、书签语义指纹、更新安装包格式和统一按钮使用约束等回归测试。

🧩 官方插件更新：

- 截图 `2.0.61`、翻译 `2.0.20`、待办 `2.0.15`、本地启动器 `2.0.34`、录屏 `2.0.62`、Git 同步 `2.0.24`、本地 AI `2.1.1`、搜索引擎 `2.0.17`。
- 上述插件最低应用版本统一为 `2.2.1`，并更新固定版本归档、完整性校验和兼容性元数据。
