🐛 问题修复：

- 修复官方插件运行时压缩后，主应用无法识别无空格 ES Module 导入语法，导致截图等插件路由注册失败、快捷键无法调出窗口的问题。
- 修复应用更新下载流临时中断后立即提示失败，需要用户多次手动点击重新下载的问题。
- 修复点击“立即安装”进入安装启动状态时，界面短暂误显示“安装失败”，但安装程序随后正常启动的问题。

🔧 优化改进：

- 插件模块加载器兼容压缩后的静态导入、重新导出及相对动态导入，并将运行时加载失败始终写入生产日志，便于定位独立窗口启动异常。
- 更新安装包下载增加最多 3 次自动尝试和退避等待，重试期间显示当前尝试次数；最终缓存前继续执行签名、SHA-256 与安装包格式校验。
- 补充压缩插件模块导入、更新自动重试退避和安装状态渲染回归测试。
