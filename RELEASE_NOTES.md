🐛 问题修复：

- 修复生产环境通过快捷键启动截图时，独立截图窗口等待全部启用插件前端加载并超过就绪超时，导致窗口无法显示的问题。
- 修复 GitHub 草稿 Release 创建成功后，资产上传阶段按标签查询返回 404 并中断发布的问题。
- 修复草稿阶段生成的 `latest.json` 使用 `untagged-*` 临时地址，正式发布后自动更新下载可能失败的问题。

🔧 优化改进：

- 动态插件路由改为优先加载当前目标路由所属插件，截图、贴图、录屏等独立窗口无需等待全部插件运行时完成，同时保留全量加载兜底。
- 截图窗口冷启动就绪保护超时由 5 秒调整为 12 秒，兼容应用刚启动或刚完成更新时的 WebView 初始化。
- 发布工作流传递并校验 Release ID，手动重试时可回退查找草稿 Release；更新元数据统一生成稳定的标签下载地址。
- 补充动态插件路由匹配、单插件冷启动、草稿 Release 解析及更新资产地址回归测试。
