🐛 问题修复：

- 修复 Windows 待机恢复后本地应用索引可能长期停留在 `0/2`，并导致快捷键、托盘菜单和退出操作无响应的问题。
- 修复截图智能吸附在鼠标停稳后从目标子元素跳转到更大父元素的问题。
- 修复 Markdown 粗体、斜体、删除线和行内代码结束后继续输入时，样式可能意外延续的问题。
- 修复 Markdown 代码块输入规则可能将光标后的普通文本一起转换为代码块，以及行内代码可能跨段落匹配反引号的问题。
- 修复 Markdown 模式切换或保存往返时，普通段落前导空格、显式换行、空代码块、空白段落和表格单元格换行可能丢失或改变语义的问题。
- 修复 Git 同步遇到短暂 `.git/index.lock` 冲突时直接失败的问题。

🔧 优化改进：

- 截图插件更新至 2.0.58；停稳检测不再用父级控件覆盖已显示的子元素，同时保留边界校正和向更小元素收敛的能力。
- 本地应用扫描增加超时、部分结果合并和来源级防重入保护；异常来源不再阻塞整个索引，并保留已有搜索数据。
- 待机期间暂停托盘系统调用，恢复后合并刷新状态，降低 Windows Shell 恢复阶段的卡死风险。
- Git 同步暂存操作增加分级退避重试，并在锁持续占用时提供更明确的处理提示。
- 补充截图吸附、Markdown 输入边界与格式往返、Git 索引锁恢复等回归测试。
