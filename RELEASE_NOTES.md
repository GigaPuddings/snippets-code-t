🎉 新增功能：

- 重构数据管理边界：Markdown 工作区继续作为片段、笔记和附件的事实源；应用配置、插件状态、可重建索引、缓存和插件包按职责分离保存。
- Git 同步采用严格白名单：同步 Markdown、受管附件及可移植偏好、快捷键和工作区设置；本地应用、浏览器书签、桌面文件、访问历史、索引、图标、绝对路径和凭证不再同步。
- 工作区新增 `.snippets-code/sync.json` 同步协议，支持字段级合并与新设备恢复；手动拉取或启动拉取后会立即导入可移植配置。
- Git 同步插件安装后会在设置中立即显示入口，并引导用户先在个人中心完成 Git 用户名、邮箱和远程仓库配置。
- 应用、桌面文件优先从 Windows Shell 读取本地图标；浏览器书签批量读取本地 favicon 数据库，减少扫描过程中的网络图标请求。

🐛 问题修复：

- 修复新设备拉取远端数据但没有文件差异时，快捷键和偏好未立即恢复的问题。
- 修复 Git 同步状态会因 `sync.json` 的 LF/CRLF 行尾转换误报为待同步的问题。
- 修复插件更新仅改界面时仍触发本地应用、书签全量重检索的问题；仅索引或提取规则版本变化才重建对应索引。
- 修复插件运行时样式资源路径解析，避免官方插件页面样式丢失。
- 修复卸载时“删除应用数据”未覆盖已记录的数据目录和插件目录的问题。
- 修复初始化向导中自定义路径输入框的主题样式，并完善设置页英文长文本的自适应布局与多语言文本。

🔧 优化改进：

- 应用、书签和桌面文件索引支持来源区分、事务替换与差量更新；重建索引、清理图标缓存和清理使用历史分别执行，不再互相级联删除。
- 快速搜索改进精确匹配与使用频率排序：近期高频项目排序更靠前，预览页打开、复制和配置跳转也会记录使用历史。
- 优化快速搜索右侧预览的滚动、长文本换行、代码区域宽度和浅色/深色主题一致性。
- 插件包与用户数据分离，普通插件更新或卸载不会删除插件配置和持久状态。
